Drop the real device clips for scene 1 in this folder.

Supported filenames:

- `work.mp4` or `work.mov` or `work.webm`
- `l.mp4` or `l.mov` or `l.webm`
- `pc.mp4` or `pc.mov` or `pc.webm`

Optional flake recordings for scene 3:

- `flake-inputs.mp4` or `flake-inputs.mov` or `flake-inputs.webm`
- `flake-outputs.mp4` or `flake-outputs.mov` or `flake-outputs.webm`

Optional mkHost recordings for scene 4:

- `mkhost-function.mp4` or `mkhost-function.mov` or `mkhost-function.webm`
- `mkhost-outputs.mp4` or `mkhost-outputs.mov` or `mkhost-outputs.webm`

Scenes auto-detect these files and fall back to a designed placeholder panel if a clip is missing.
